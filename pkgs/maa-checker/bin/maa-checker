#!/usr/bin/env node
import * as path from 'node:path'

import { runCli } from '../dist/index.mjs'

runCli(process.argv[2], process.argv[3] ?? path.join(process.cwd(), 'maachecker.config.mts')).then(
  succ => {
    process.exit(succ ? 0 : 1)
  }
)
